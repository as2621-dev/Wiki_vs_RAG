# Wiki vs RAG benchmark harness — Python 3.12+
anthropic>=0.49.0
voyageai>=0.3.0
pinecone>=5.0.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
# Optional movie-script fallback: lazily imported in corpus.py; only needed for a live
# Playwright render (tests mock it). Browser binaries via `playwright install chromium`.
playwright>=1.44.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
structlog>=24.1.0
